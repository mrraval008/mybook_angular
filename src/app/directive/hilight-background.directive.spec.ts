import { HilightBackgroundDirective } from './hilight-background.directive';

describe('HilightBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
